<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administration Portal</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <link rel="stylesheet" href="/public/css/style.css">

    <style>

    </style>
</head>

<body class="bg-[#020617] text-white">

    <canvas id="bg-canvas" class="fixed top-0 left-0 w-full h-full -z-10"></canvas>

    <div class="flex h-screen w-full p-4 gap-4 relative z-10">

        <!-- sidebar  -->
        <aside class="w-72 glass rounded-3xl flex flex-col sidebar-anim shrink-0">
            <div class="p-8 text-center">
                <div
                    class="w-16 h-16 bg-blue-600 rounded-2xl mx-auto mb-4 flex items-center justify-center shadow-lg shadow-blue-500/20">
                    <i class="fas fa-atom text-2xl"></i>
                </div>
                <h1 class="font-bold tracking-widest text-xl text-blue-400 uppercase">Admin Core</h1>
            </div>

            <nav class="flex-grow px-4 space-y-2">
                <button
                    class="nav-btn w-full flex items-center space-x-4 p-4 rounded-xl text-gray-300 hover:bg-white/5 transition-all">
                    <i class="fas fa-th-list"></i>
                    <span>View All Admins</span>
                </button>
                <button
                    class="nav-btn w-full flex items-center space-x-4 p-4 rounded-xl text-gray-300 hover:bg-white/5 transition-all">
                    <i class="fas fa-user-plus"></i>
                    <a href="/addNewAdmin">
                        <span>Add New Admin</span>
                    </a>
                </button>
                <button
                    class="nav-btn w-full flex items-center space-x-4 p-4 rounded-xl text-gray-300 hover:bg-white/5 transition-all">
                    <i class="fas fa-fingerprint"></i>
                    <span>Change Password</span>
                </button>
            </nav>

            <div class="p-6">
                <button
                    class="w-full p-4 rounded-2xl bg-red-500/10 hover:bg-red-500 text-red-400 hover:text-white border border-red-500/20 transition-all">
                    <i class="fas fa-power-off mr-2"></i> Logout
                </button>
            </div>
        </aside>

        <div class="flex-grow flex flex-col gap-4">

            <!-- header  -->
            <header class="h-20 glass rounded-3xl flex items-center justify-between px-8 header-anim shrink-0">
                <div class="flex items-center gap-3">
                    <div class="h-2 w-2 bg-blue-500 rounded-full animate-pulse"></div>
                    <span class="text-xs font-mono opacity-50 tracking-widest uppercase">System: Active</span>
                </div>
                <div class="flex items-center gap-4">
                    <div class="text-right">
                        <p class="text-sm font-bold leading-none">Admin User</p>
                        <p class="text-[10px] opacity-40 uppercase mt-1">Root Access</p>
                    </div>
                    <div class="h-10 w-10 rounded-full bg-gradient-to-tr from-blue-600 to-indigo-600 p-[2px]">
                        <div class="w-full h-full rounded-full bg-[#020617] flex items-center justify-center">
                            <i class="fas fa-user text-xs"></i>
                        </div>
                    </div>
                </div>
            </header>